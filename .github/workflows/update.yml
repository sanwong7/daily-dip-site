name: Daily Stock Screener

on:
  push:                   # 🔥 新增：當有代碼更新(push)時，自動執行
    branches: [ "main" ]
  schedule:
    - cron: '0 21 * * 1-5' # 每天美股收盤後執行
  workflow_dispatch:      # 允許手動按鈕觸發

permissions:              # 🔥 關鍵：給予寫入權限
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install yfinance mplfinance pandas numpy requests lxml

      - name: Run analysis script
        env:
          POLYGON_API_KEY: ${{ secrets.POLYGON_API_KEY }}
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: python main.py

      - name: Commit and Push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          # 1. 先把所有變動暫存起來
          git add .
          git stash
          
          # 2. 強制拉取最新版 (Rebase)
          git pull --rebase origin main
          
          # 3. 把暫存的變動放回來 (如果有衝突，優先使用我們剛生成的)
          git stash pop || echo "No stash to pop"
          
          # 4. 再次加入所有檔案並提交
          git add .
          git commit -m "Update daily stock data & history" || echo "No changes to commit"
          
          # 5. 推送
          git push origin main
