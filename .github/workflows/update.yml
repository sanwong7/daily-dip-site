name: Daily Stock Screener

on:
  schedule:
    # 這裡設定每天自動執行的時間 (例如 UTC 21:00 = 台灣時間清晨 05:00)
    - cron: '0 21 * * 1-5'
  workflow_dispatch: # 允許手動按鈕觸發

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          # 🔥 確保這裡有 requests
          pip install yfinance mplfinance pandas numpy requests lxml

      - name: Run analysis script
        # 🔥 這裡是關鍵！我們要告訴 GitHub 把 Secrets 傳進去
        env:
          POLYGON_API_KEY: ${{ secrets.POLYGON_API_KEY }}
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: python main.py

      - name: Commit and Push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add index.html
          # 如果沒有變更就不 Commit，避免報錯
          git commit -m "Update daily stock data" || echo "No changes to commit"
          git push
